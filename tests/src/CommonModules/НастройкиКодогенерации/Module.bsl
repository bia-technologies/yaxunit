//©///////////////////////////////////////////////////////////////////////////©//
//
//  Copyright 2021-2025 BIA-Technologies Limited Liability Company
//
//  Licensed under the Apache License, Version 2.0 (the "License");
//  you may not use this file except in compliance with the License.
//  You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
//  Unless required by applicable law or agreed to in writing, software
//  distributed under the License is distributed on an "AS IS" BASIS,
//  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//  See the License for the specific language governing permissions and
//  limitations under the License.
//
//©///////////////////////////////////////////////////////////////////////////©//

#Область ПрограммныйИнтерфейс

// Возвращает соответствие типов данных и кода для их инициализации при генерации кода создания тестовых данных.
// Используется модулем ЮТГенераторКодаСлужебныйСервер для формирования выражений создания объектов
// определенных типов вместо использования фиктивных значений или поиска по базе данных.
//
// Возвращаемое значение:
//  Соответствие из Строка - соответствие типов данных и кода для их установки:
//    * Ключ - Тип - тип значения переменной
//    * Значение - Строка - код установки значения, должен содержать плейсхолдер %1 для подстановки строкового представления значения
//
// Пример:
//  КодИнициализации = Новый Соответствие;
//  КодИнициализации.Вставить(Тип("СправочникСсылка.РасчетныеСчетаКонтрагентов"), "ТестовыеДанные.СчетКонтрагента(""%1"", Контрагент)");
//
Функция КодИнициализацииПоТипам() Экспорт
	
	КодИнициализации = Новый Соответствие;
	КодИнициализации.Вставить(Тип("СправочникСсылка.РасчетныеСчетаКонтрагентов"), "ТестовыеДанные.СчетКонтрагента(""%1"", Контрагент)");
	
	Возврат КодИнициализации;
	
КонецФункции

// Возвращает строку с выражением для получения значения определенного типа при генерации кода.
// Используется модулем ЮТГенераторКодаСлужебныйСервер для формирования кода создания объектов,
// когда требуется специальная логика получения значения, отличная от стандартной генерации.
//
// Параметры:
//  Значение - Произвольный - значение, для которого формируется выражение получения
//  ТипЗначения - Тип - тип значения переменной
//
// Возвращаемое значение:
//  Строка, Неопределено - строка с кодом получения значения или Неопределено, если для данного типа
//                         не определено специальное выражение
//
// Пример:
//  Если ТипЗначения = Тип("СправочникСсылка.Организации") Тогда
//    Возврат "ТестовыеДанные.ТестоваяОрганизация()";
//  КонецЕсли;
//
Функция ВыражениеПолученияЗначения(Значение, ТипЗначения) Экспорт
	
	Если ТипЗначения = Тип("СправочникСсылка.Организации") Тогда
		Возврат "ТестовыеДанные.ТестоваяОрганизация()";
	КонецЕсли;
	
КонецФункции

// Возвращает соответствие типов данных и их порядкового номера для сортировки переменных при генерации кода.
// Используется модулем ЮТГенераторКодаСлужебныйСервер для упорядочивания переменных в сгенерированном коде.
// Переменные с меньшим порядковым номером будут размещены в коде раньше переменных с большим номером.
//
// Возвращаемое значение:
//  Соответствие из Число - соответствие типов значений и их порядка для сортировки:
//    * Ключ - Тип - тип значения переменной
//    * Значение - Число - порядковый номер для сортировки (меньшее значение = выше в коде)
//
// Пример:
//  Порядок = Новый Соответствие;
//  Порядок.Вставить(Тип("СправочникСсылка.Организации"), 0);
//
Функция ПорядокСортировкиПеременных() Экспорт
	
	Порядок = Новый Соответствие;
	Возврат Порядок;
	
КонецФункции

// Возвращает соответствие типов данных и способов их поиска для постоянных данных системы.
// Используется модулем ЮТГенераторКодаСлужебныйСервер для формирования кода поиска объектов
// по постоянным идентификаторам (код или наименование) вместо создания новых объектов.
//
// Возвращаемое значение:
//  Соответствие из Строка - соответствие типов данных и способов их поиска:
//    * Ключ - Тип - тип значения переменной
//    * Значение - Строка - вариант поиска элемента справочника:
//        + "Код" - поиск по коду элемента
//        + "Наименование" - поиск по наименованию элемента
//
// Пример:
//  ПостоянныеДанные = Новый Соответствие;
//  ПостоянныеДанные.Вставить(Тип("СправочникСсылка.Валюты"), "Код");
//
Функция ПостоянныеДанные() Экспорт
	
	ПостоянныеДанные = Новый Соответствие;
	Возврат ПостоянныеДанные;
	
КонецФункции

// Возвращает соответствие имен реквизитов и кода для их установки по умолчанию при генерации кода.
// Используется модулем ЮТГенераторКодаСлужебныйСервер для замены значений реквизитов
// на специальные выражения вместо использования фактических значений из базы данных.
//
// Возвращаемое значение:
//  Соответствие из Строка - соответствие имен реквизитов и кода для их установки:
//    * Ключ - Строка - имя реквизита объекта
//    * Значение - Строка - код установки значения реквизита
//
// Пример:
//  ЗаменяемыеРеквизиты = Новый Соответствие;
//  ЗаменяемыеРеквизиты.Вставить("Дата", "ТекущаяДатаСеанса()");
//  ЗаменяемыеРеквизиты.Вставить("Номер", "ЮТест.Данные().СлучайнаяСтрока(20,,""0123456789"")");
//
Функция ЗаменаЗначенийРеквизитовПоУмолчанию() Экспорт
	
	ЗаменяемыеРеквизиты = Новый Соответствие;
	
	ЗаменяемыеРеквизиты.Вставить("Дата", "ТекущаяДатаСеанса()");
	ЗаменяемыеРеквизиты.Вставить("Номер", "ЮТест.Данные().СлучайнаяСтрока(20,,""0123456789"")");
	
	Возврат ЗаменяемыеРеквизиты;
	
КонецФункции

#КонецОбласти
