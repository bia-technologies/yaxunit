//©///////////////////////////////////////////////////////////////////////////©//
//
//  Copyright 2021-2025 BIA-Technologies Limited Liability Company
//
//  Licensed under the Apache License, Version 2.0 (the "License");
//  you may not use this file except in compliance with the License.
//  You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
//  Unless required by applicable law or agreed to in writing, software
//  distributed under the License is distributed on an "AS IS" BASIS,
//  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//  See the License for the specific language governing permissions and
//  limitations under the License.
//
//©///////////////////////////////////////////////////////////////////////////©//

#Область ПрограммныйИнтерфейс

// Ищет и возвращает ссылку на объект по значению его реквизита.
//
// Параметры:
//  МетаданныеОбъекта - ОбъектМетаданных - Явное указание объекта метаданных.
//                    - Произвольный - менеджер, например, Справочники.Номенклатура.
//                    - Строка -  Полное имя объекта метаданных или менеджера, например, "Справочник.Номенклатура".
//                    - Тип, ОписаниеТипов - Тип искомого объекта
//  ИмяРеквизита      - Строка       - Имя реквизита, по которому выполняется поиск.
//  ЗначениеРеквизита - Произвольный - Значение реквизита для поиска.
//
// Возвращаемое значение:
//  ЛюбаяСсылка, Неопределено - Ссылка на найденный объект или Неопределено, если объект не найден.
//
// Пример:
//  // Поиск контрагента по ИНН
//  ИНН = "1234567890";
//  СсылкаНаКонтрагента = ЮТПоискДанных.СсылкаПоРеквизиту("Справочник.Контрагенты", "ИНН", ИНН);
//
//  Если СсылкаНаКонтрагента <> Неопределено Тогда
//      Сообщить("Найден контрагент: " + СсылкаНаКонтрагента);
//  Иначе
//      Сообщить("Контрагент с ИНН " + ИНН + " не найден.");
//  КонецЕсли;
//
Функция СсылкаПоРеквизиту(МетаданныеОбъекта, ИмяРеквизита, ЗначениеРеквизита) Экспорт
#Если Сервер Тогда
	Возврат ЮТПоискДанныхСлужебныйСервер.СсылкаПоРеквизиту(МетаданныеОбъекта, ИмяРеквизита, ЗначениеРеквизита);
#Иначе
	Возврат ЮТПоискДанныхСлужебныйВызовСервера.СсылкаПоРеквизиту(МетаданныеОбъекта, ИмяРеквизита, ЗначениеРеквизита);
#КонецЕсли
КонецФункции

// Возвращает случайную ссылку на объект из информационной базы,
// удовлетворяющую заданным условиям отбора.
//
// Параметры:
//  МетаданныеОбъекта - ОбъектМетаданных - Явное указание объекта метаданных.
//                    - Произвольный - менеджер, например, Справочники.Номенклатура.
//                    - Строка -  Полное имя объекта метаданных или менеджера, например, "Справочник.Номенклатура".
//                    - Тип, ОписаниеТипов - Тип искомого объекта
//  Отборы - Массив из см. ЮТФабрика.ВыражениеПредиката - Набор условий, см. ЮТПредикаты.Получить
//           - см. ЮТФабрика.ВыражениеПредиката
//           - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат
//           - Структура, Соответствие - Простой отбор по полям "ключ-значение".
//           - Неопределено - Отбор не используется.
//
// Возвращаемое значение:
//  ЛюбаяСсылка, Неопределено - Случайная ссылка на найденный объект или Неопределено, если ничего не найдено.
//
// Пример:
//  // Поиск случайного договора с видом "СПокупателем"
//  Отбор = Новый Структура;
//  Отбор.Вставить("ВидДоговора", Перечисления.ВидыДоговоровКонтрагентов.СПокупателем);
//
//  СлучайныйДоговор = ЮТПоискДанных.СлучайнаяСсылка("Справочник.ДоговорыКонтрагентов", Отбор);
//
//  Если СлучайныйДоговор <> Неопределено Тогда
//      Сообщить("Найден случайный договор: " + СлучайныйДоговор);
//  КонецЕсли;
//
Функция СлучайнаяСсылка(МетаданныеОбъекта, Отборы = Неопределено) Экспорт
	
	Возврат ЮТЗапросы.СлучайнаяЗапись(МетаданныеОбъекта, Отборы);
	
КонецФункции

// Ищет и возвращает ссылку на объект по значению,
// которое проверяется по стандартным реквизитам (Код, Наименование) и основному представлению.
//
// Параметры:
//  МетаданныеОбъекта - ОбъектМетаданных - Явное указание объекта метаданных.
//                    - Произвольный - менеджер, например, Справочники.Номенклатура.
//                    - Строка -  Полное имя объекта метаданных или менеджера, например, "Справочник.Номенклатура".
//                    - Тип, ОписаниеТипов - Тип искомого объекта
//  Значение          - Строка, Число, УникальныйИдентификатор - Значение для поиска.
//  Отборы - Массив из см. ЮТФабрика.ВыражениеПредиката - Набор условий, см. ЮТПредикаты.Получить
//           - см. ЮТФабрика.ВыражениеПредиката
//           - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат
//           - Структура, Соответствие - Простой отбор по полям "ключ-значение".
//           - Неопределено - Отбор не используется.
//
// Возвращаемое значение:
//  ЛюбаяСсылка, Неопределено - Ссылка на найденный объект или Неопределено, если объект не найден.
//
// Пример:
//  // Поиск организации по наименованию
//  ИмяОрганизации = "Главная организация";
//  СсылкаНаОрганизацию = ЮТПоискДанных.Ссылка("Справочник.Организации", ИмяОрганизации);
//
//  Если СсылкаНаОрганизацию <> Неопределено Тогда
//      Сообщить("Найдена организация: " + СсылкаНаОрганизацию);
//  КонецЕсли;
//
//  // Поиск пользователя по коду с дополнительным отбором
//  КодПользователя = "00-000001";
//  Отбор = Новый Структура("Недействителен", Ложь);
//  СсылкаНаПользователя = ЮТПоискДанных.Ссылка(Справочники.Пользователи, КодПользователя, Отбор);
//
//  Если СсылкаНаПользователя <> Неопределено Тогда
//      Сообщить("Найден действующий пользователь: " + СсылкаНаПользователя);
//  КонецЕсли;
//
Функция Ссылка(МетаданныеОбъекта, Значение, Знач Отборы = Неопределено) Экспорт
#Если Сервер Тогда
	Возврат ЮТПоискДанныхСлужебныйСервер.Ссылка(МетаданныеОбъекта, Значение, Отборы);
#Иначе
	Если ЮТПредикатыСлужебныйКлиентСервер.ЭтоПредикатОбщийМодуль(Отборы) Тогда
		Отборы = Отборы.Получить();
	КонецЕсли;
	Возврат ЮТПоискДанныхСлужебныйВызовСервера.Ссылка(МетаданныеОбъекта, Значение, Отборы);
#КонецЕсли
КонецФункции

#КонецОбласти

